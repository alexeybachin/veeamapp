{
  "name": "veeamapp",
  "version": "1.0.0",
  "description": "App that places markers over an image as a task for Veeam",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "parcel -p 8888 src/index.html",
    "build": "parcel build src/index.html --out-dir dist --no-cache"
  },
  "author": "Bachin",
  "license": "MIT",
  "devDependencies": {
    "autoprefixer": "^9.7.3",
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.3",
    "babel-preset-env": "^1.7.0",
    "node-sass": "^4.13.0",
    "parcel-bundler": "^1.12.4"
  },
  "dependencies": {
    "normalize.css": "^8.0.1",
    "src": "^1.1.2"
  }
}
